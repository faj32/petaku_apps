<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F5F5">

    <!-- PETA: full screen di belakang -->
    <WebView
        android:id="@+id/mapWebView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- PANEL ATAS: floating di atas peta -->
    <LinearLayout
        android:id="@+id/panelAtas"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="#4CAF50"
        android:elevation="8dp">

        <!-- Baris input + tombol (bisa disembunyikan) -->
        <LinearLayout
            android:id="@+id/kontenPanel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:paddingTop="12dp"
            android:paddingBottom="8dp">

            <!-- Input Alamat dengan ikon mobil -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:orientation="horizontal"
                android:background="#FFFFFF"
                android:gravity="center_vertical"
                android:paddingStart="12dp"
                android:paddingEnd="12dp">

                <ImageView
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:src="@android:drawable/ic_menu_directions"
                    app:tint="#4CAF50"
                    android:contentDescription="Ikon Arah"
                    android:layout_marginEnd="8dp" />

                <EditText
                    android:id="@+id/inputLokasi"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1"
                    android:background="@null"
                    android:hint="@string/hint_alamat"
                    android:autofillHints="streetAddress"
                    android:textColorHint="#AAAAAA"
                    android:textColor="#000000"
                    android:textSize="14sp"
                    android:inputType="text"
                    android:imeOptions="actionDone"
                    android:maxLines="1" />
            </LinearLayout>

            <!-- Tombol Rute Optimal -->
            <Button
                android:id="@+id/btnProses"
                android:layout_width="match_parent"
                android:layout_height="44dp"
                android:layout_marginTop="10dp"
                android:layout_marginBottom="4dp"
                android:backgroundTint="#FFFFFF"
                android:text="@string/btn_rute_optimal"
                android:textColor="#2E7D32"
                android:textStyle="bold"
                android:textSize="13sp"
                app:cornerRadius="4dp" />

        </LinearLayout>

        <!-- Tombol collapse/expand (panah) -->
        <LinearLayout
            android:id="@+id/btnTogglePanel"
            android:layout_width="match_parent"
            android:layout_height="28dp"
            android:gravity="center"
            android:orientation="horizontal"
            android:background="#43A047"
            android:clickable="true"
            android:focusable="true">

            <ImageView
                android:id="@+id/iconPanah"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:src="@android:drawable/arrow_up_float"
                android:contentDescription="Toggle Panel"
                app:tint="#FFFFFF" />
        </LinearLayout>

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
